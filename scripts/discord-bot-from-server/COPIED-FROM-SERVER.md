# Discord Bot Files (Copied from Server)

These files were copied from **moodmnky@10.3.0.119:~/POKE-MNKY/tools/discord-bot/** for reference and migration. The bot on the server runs as a Docker service and uses the Discord Gateway (WebSocket); the app now also supports an **Interactions Endpoint** (Supabase Edge Function) that receives HTTP POSTs from Discord.

- **Source:** `~/POKE-MNKY/tools/discord-bot/` on 10.3.0.119  
- **Copied:** All source and config files; `node_modules` was excluded (run `pnpm install` here if you need to run this bot locally).  
- **See:** [SUPABASE-DISCORD-BOT-SETUP.md](../../SUPABASE-DISCORD-BOT-SETUP.md) for how this relates to the Supabase discord-interactions function and Developer Portal URLs.
